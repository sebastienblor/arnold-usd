
This repository contains dependencies for Solid Angle projects. There will be a directory for every different version of the same library.

When necessary, there will be a different directory for each supported platform. For example:

libtiff-3.8.2
  src
  dist
    include
    lib
      linux_x86
      linux_x86_64
      win32
