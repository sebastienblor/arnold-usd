file -f -o "test.ma";
arnoldRender -cam persp;
pause -sec 5;
arnoldRenderView;
pause -sec 3;
arnoldRenderView -option "Wait Render"  "1";

string $imgOut = `pwd`;
$imgOut += "/testrender.tif";
arnoldRenderView -option "Save Image" $imgOut;
quit -force -abort;
